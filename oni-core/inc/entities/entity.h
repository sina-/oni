#pragma once

#include <components/component.h>

namespace oni {
    namespace entities {
//        const static components::Mask Sprite = components::Mask().set(components::PLACEMENT).set(components::APPEARANCE);
//        const static components::Mask DynamicSprite = components::Mask().set(components::PLACEMENT).set(components::APPEARANCE).set(components::DYNAMIC);
    }
}
