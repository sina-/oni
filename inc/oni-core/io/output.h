#pragma once

#include <string>

namespace oni {
    namespace io {
        void printl(const std::string &text);

        void printl(int text);

        void printl(double text);

        void printl(float text);
    }
}
